{
  "name": "puhelinluettelo-backend",
  "version": "1.0.0",
  "description": "server side code",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../puhelinluettelo-front/ && npm run build && cp -r build ../puhelinluettelo-backend",
    "deploy": "git push heroku mater",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail",
    "lint": "eslint ."
  },
  "keywords": [
    "express",
    "mongodb"
  ],
  "author": "kim",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.5",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.10.0",
    "nodemon": "^2.0.15"
  }
}
